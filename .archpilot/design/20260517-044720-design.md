# Solution Design

## Summary

This design document provides a comprehensive, implementation-ready architecture for the **Product Catalog Management System** — a secure, scalable admin platform for managing product inventory, categories, and catalog visibility on AWS.

### What's Included

This design synthesizes multiple architectural concerns into one cohesive solution:

1. **Single Page Application (SPA) Frontend Design** — React-based admin panel with TypeScript, Tailwind CSS, and Zustand for state management
2. **REST API Backend Design** — Node.js/Express API with Lambda or ECS Fargate, enforcing RBAC and business rules
3. **Relational Database Design** — PostgreSQL schema for products, categories, images, and audit trails
4. **AWS Infrastructure Architecture** — Cognito, API Gateway, Lambda, RDS, S3, CloudFront, DynamoDB, SQS, EventBridge
5. **Cross-Cutting Concerns** — Authentication, authorization, observability, error handling, performance optimization

### Table of Contents

- **Technology Stack & Architecture Pattern**
- **Frontend Design (SPA)**
  - Component hierarchy, routing, state management, API integration
- **Backend API Design**
  - Endpoint surface, service layers, validation, authentication/authorization
- **Database Design**
  - Schema, relationships, indexing strategy, migration approach
- **AWS Infrastructure Design**
  - Compute, storage, messaging, monitoring, security services
- **Integration & Background Jobs**
  - Image processing, malware scanning, bulk operations, alerts, exports
- **Security & Compliance**
  - Encryption, RBAC, audit logging, rate limiting
- **Observability & Operations**
  - Logging, tracing, metrics, alerting, backup/restore
- **Deployment Strategy**
  - CI/CD pipeline, blue/green deployment, rollback plan
- **Design Decisions & Trade-offs**

**Note:** Diagram and Cost sections are generated separately after this base design is reviewed.

---

## New Design

# Product Catalog Management System — Implementation-Ready Design

## Technology Stack & Architecture Pattern

### Frontend Stack
- **Framework**: React 18 with TypeScript
- **Build Tool**: Vite (fast dev server, optimized production builds)
- **Styling**: Tailwind CSS (utility-first, responsive design)
- **State Management**: Zustand (lightweight global state) + TanStack Query (server state/cache)
- **Routing**: React Router v6 (declarative routing, lazy loading)
- **Forms**: React Hook Form + Zod (validation schema)
- **API Client**: Axios with interceptors for auth and error handling
- **Testing**: Vitest + React Testing Library (unit and integration tests)
- **Accessibility**: Radix UI primitives (WCAG 2.1 AA compliant components)

### Backend Stack
- **Runtime**: Node.js 20 LTS
- **Framework**: Express.js (REST API)
- **Language**: TypeScript
- **Compute**: AWS Lambda (serverless, auto-scaling) or ECS Fargate (containerized, long-running)
- **API Gateway**: AWS API Gateway (REST API, throttling, caching, custom domain)
- **Authentication**: AWS Cognito (user pool, MFA, JWT tokens)
- **Validation**: Zod (schema validation for request/response)
- **ORM**: Prisma (type-safe database client, migrations)
- **Testing**: Jest + Supertest (unit and integration tests)

### Database & Storage
- **Primary Database**: Amazon RDS PostgreSQL 15 (Multi-AZ, automated backups)
- **Audit Logs**: Amazon DynamoDB (append-only, point-in-time recovery)
- **Object Storage**: Amazon S3 (product images, exports)
- **CDN**: Amazon CloudFront (global image delivery, caching)
- **Cache**: Amazon ElastiCache (Redis) — optional for search/filter caching

### Infrastructure & DevOps
- **IaC**: AWS CDK (TypeScript) or Terraform
- **CI/CD**: GitHub Actions or AWS CodePipeline + CodeBuild + CodeDeploy
- **Monitoring**: Amazon CloudWatch (logs, metrics, dashboards, alarms)
- **Tracing**: AWS X-Ray (distributed tracing)
- **Security**: AWS WAF (DDoS protection), AWS Secrets Manager (credentials), AWS KMS (encryption keys)
- **Messaging**: Amazon SQS (FIFO queues for bulk operations)
- **Scheduler**: Amazon EventBridge (scheduled jobs for alerts, exports, backups)

### Architecture Pattern

**Modular Monolith with Serverless Components**

- **Web UI**: Single-page React app hosted on S3 + CloudFront (static assets)
- **API Layer**: Express.js REST API running on Lambda (serverless) or ECS Fargate (containerized)
- **Service Layer**: Domain services (ProductService, CategoryService, AuditLogService) encapsulate business logic
- **Data Layer**: Prisma ORM for PostgreSQL access; DynamoDB SDK for audit logs
- **Background Jobs**: Lambda functions triggered by SQS, S3 events, or EventBridge schedules
- **API Gateway**: Single entry point for all API requests; handles auth, throttling, caching

**Why Modular Monolith?**
- Simpler deployment and debugging for MVP
- Shared domain logic and database transactions
- Easy to extract microservices later if needed (e.g., ImageProcessingService, BulkOperationService)
- Reduced operational complexity compared to microservices

---

## Frontend Design (SPA)

### Application Architecture

**Feature-Based Folder Structure**

```
src/
├── app/                          # App entry, providers, router
│   ├── App.tsx                   # Root component
│   ├── router.tsx                # React Router configuration
│   └── providers.tsx             # AuthProvider, QueryProvider, ThemeProvider
├── features/                     # Feature modules (domain-driven)
│   ├── auth/
│   │   ├── components/           # LoginForm, MFAChallenge
│   │   ├── hooks/                # useAuth, useLogin, useMFA
│   │   ├── services/             # authService (API calls)
│   │   └── types.ts              # Auth types
│   ├── products/
│   │   ├── components/           # ProductList, ProductForm, ProductCard
│   │   ├── hooks/                # useProducts, useCreateProduct, useUpdateProduct
│   │   ├── services/             # productService (API calls)
│   │   └── types.ts              # Product types
│   ├── categories/
│   │   ├── components/           # CategoryTree, CategoryForm
│   │   ├── hooks/                # useCategories, useCategoryTree
│   │   ├── services/             # categoryService (API calls)
│   │   └── types.ts              # Category types
│   ├── audit-logs/
│   │   ├── components/           # AuditLogTable, AuditLogFilters
│   │   ├── hooks/                # useAuditLogs, useExportLogs
│   │   ├── services/             # auditLogService (API calls)
│   │   └── types.ts              # AuditLog types
│   └── bulk-operations/
│       ├── components/           # BulkActionModal, ProgressIndicator
│       ├── hooks/                # useBulkOperation, useBulkStatus
│       ├── services/             # bulkOperationService (API calls)
│       └── types.ts              # BulkOperation types
├── components/                   # Shared UI components
│   ├── ui/                       # Atomic components (Button, Input, Modal, Card, Table)
│   ├── layout/                   # Header, Sidebar, MainLayout
│   └── forms/                    # FormField, FormError, ImageUpload
├── hooks/                        # Shared custom hooks
│   ├── useDebounce.ts
│   ├── useLocalStorage.ts
│   └── usePermissions.ts
├── services/                     # API client and interceptors
│   ├── api.ts                    # Axios instance with interceptors
│   └── upload.ts                 # Presigned URL upload logic
├── stores/                       # Zustand stores
│   ├── authStore.ts              # User session, permissions
│   └── uiStore.ts                # UI state (sidebar open, theme)
├── types/                        # Shared TypeScript types
│   ├── api.ts                    # API request/response types
│   └── domain.ts                 # Domain model types
├── utils/                        # Utility functions
│   ├── formatters.ts             # Date, currency, number formatters
│   ├── validators.ts             # Client-side validation helpers
│   └── constants.ts              # App constants (roles, statuses)
└── styles/                       # Global styles, Tailwind config
    ├── globals.css
    └── tailwind.config.ts
```

### Component Hierarchy

```
App
├── AuthProvider (Zustand + Cognito session)
├── QueryClientProvider (TanStack Query)
├── ThemeProvider (light/dark mode)
└── Router
    ├── PublicRoutes
    │   └── LoginPage
    │       └── LoginForm
    │           ├── EmailInput
    │           ├── PasswordInput
    │           └── MFAChallenge (conditional)
    └── ProtectedRoutes (requires auth)
        └── MainLayout
            ├── Header
            │   ├── UserMenu
            │   └── NotificationBell
            ├── Sidebar
            │   └── Navigation (Products, Categories, Audit Logs, Settings)
            └── MainContent
                ├── DashboardPage
                │   ├── StatsCards (total products, low stock, drafts)
                │   ├── ProductList (table with search/filter)
                │   └── QuickActions (Create Product, Bulk Edit)
                ├── ProductCreatePage
                │   └── ProductForm
                │       ├── BasicInfoSection (name, description, SKU, price)
                │       ├── CategorySelector (multi-select)
                │       ├── ImageUpload (drag-and-drop, preview, reorder)
                │       ├── InventorySection (stock, low-stock threshold)
                │       ├── VisibilityToggle (draft/published)
                │       └── SubmitButton
                ├── ProductEditPage
                │   └── ProductForm (same as create, pre-filled)
                ├── ProductDetailPage
                │   ├── ProductInfo
                │   ├── ImageGallery
                │   ├── AuditHistory
                │   └── ActionButtons (Edit, Delete)
                ├── CategoriesPage
                │   ├── CategoryTree (hierarchical, drag-and-drop)
                │   ├── CategoryForm (create/edit modal)
                │   └── CategoryActions (Edit, Delete)
                ├── AuditLogsPage
                │   ├── AuditLogFilters (date range, user, action)
                │   ├── AuditLogTable (paginated)
                │   └── ExportButton (CSV)
                ├── BulkOperationsModal
                │   ├── ProductSelector (checkboxes)
                │   ├── ActionSelector (edit, delete, categorize)
                │   ├── ProgressIndicator
                │   └── ConfirmButton
                └── SettingsPage
                    ├── UserProfile
                    ├── MFASettings
                    └── Preferences
```

### Page Designs

#### DashboardPage (`/`)
- **Route**: `/`
- **Purpose**: Overview of product catalog with quick stats and product list
- **Components**: StatsCards, ProductList (table), QuickActions
- **State**: Product list (paginated, filtered), search query, selected filters
- **API Calls**: `GET /api/v1/products`, `GET /api/v1/categories`
- **User Interactions**: Search products, apply filters, navigate to create/edit, select products for bulk actions

#### LoginPage (`/login`)
- **Route**: `/login`
- **Purpose**: Admin authentication with email/password and MFA
- **Components**: LoginForm, MFAChallenge
- **State**: Email, password, MFA code, loading state, error message
- **API Calls**: `POST /auth/login` (Cognito)
- **User Interactions**: Enter credentials, submit, complete MFA challenge, redirect to dashboard

#### ProductCreatePage (`/products/new`)
- **Route**: `/products/new`
- **Purpose**: Create new product with all required fields and images
- **Components**: ProductForm (BasicInfoSection, CategorySelector, ImageUpload, InventorySection, VisibilityToggle)
- **State**: Form data (name, description, SKU, price, categories, images, stock, status), validation errors, loading state
- **API Calls**: `POST /api/v1/images/presigned-url`, `POST /api/v1/products`
- **User Interactions**: Fill form, upload images, select categories, submit, handle validation errors, redirect to product detail on success

#### ProductEditPage (`/products/:id/edit`)
- **Route**: `/products/:id/edit`
- **Purpose**: Edit existing product with optimistic locking
- **Components**: ProductForm (pre-filled with existing data)
- **State**: Form data, validation errors, loading state, version (for optimistic locking)
- **API Calls**: `GET /api/v1/products/:id`, `PUT /api/v1/products/:id`, `POST /api/v1/images/presigned-url`
- **User Interactions**: Modify fields, upload/reorder images, submit, handle concurrent edit conflicts (409 Conflict), redirect to product detail on success

#### ProductDetailPage (`/products/:id`)
- **Route**: `/products/:id`
- **Purpose**: View product details, images, and audit history
- **Components**: ProductInfo, ImageGallery, AuditHistory, ActionButtons
- **State**: Product data, audit logs (paginated)
- **API Calls**: `GET /api/v1/products/:id`, `GET /api/v1/audit-logs?product_id=:id`
- **User Interactions**: View details, navigate to edit, delete product (with confirmation), view audit history

#### CategoriesPage (`/categories`)
- **Route**: `/categories`
- **Purpose**: Manage category hierarchy with drag-and-drop reordering
- **Components**: CategoryTree, CategoryForm (modal), CategoryActions
- **State**: Category tree (hierarchical), selected category, form data (name, parent_id, order)
- **API Calls**: `GET /api/v1/categories`, `POST /api/v1/categories`, `PUT /api/v1/categories/:id`, `DELETE /api/v1/categories/:id`
- **User Interactions**: View tree, create category, edit category, delete category, drag-and-drop to reorder or change parent

#### AuditLogsPage (`/audit-logs`)
- **Route**: `/audit-logs`
- **Purpose**: View and export audit trail of all administrative actions
- **Components**: AuditLogFilters, AuditLogTable (paginated), ExportButton
- **State**: Audit logs (paginated, filtered), filter criteria (date range, user, action)
- **API Calls**: `GET /api/v1/audit-logs`, `POST /api/v1/audit-logs/export`
- **User Interactions**: Apply filters, view logs, export to CSV, navigate to related product/category

#### BulkOperationsModal (overlay)
- **Route**: N/A (modal overlay on DashboardPage)
- **Purpose**: Perform bulk actions on selected products
- **Components**: ProductSelector, ActionSelector, ProgressIndicator, ConfirmButton
- **State**: Selected product IDs, bulk action (edit/delete/categorize), job ID, job status, progress percentage
- **API Calls**: `POST /api/v1/products/bulk`, `GET /api/v1/products/bulk/:job_id/status`
- **User Interactions**: Select products, choose action, confirm, monitor progress, close modal on completion

#### SettingsPage (`/settings`)
- **Route**: `/settings`
- **Purpose**: Manage user profile, MFA, and preferences
- **Components**: UserProfile, MFASettings, Preferences
- **State**: User data (name, email, role), MFA status, preferences (theme, notifications)
- **API Calls**: `GET /api/v1/users/me`, `PUT /api/v1/users/me`, `POST /auth/mfa/enable`
- **User Interactions**: Update profile, enable/disable MFA, change preferences

### State Management Design

#### Global State (Zustand)

| State Slice | Data | Update Triggers | Persistence |
|---|---|---|---|
| `authStore` | `user`, `token`, `refreshToken`, `roles`, `isAuthenticated` | Login, logout, token refresh | localStorage (encrypted) |
| `uiStore` | `sidebarOpen`, `theme`, `notifications` | User interactions, system events | localStorage |

#### Server State (TanStack Query)

**Queries** (cached data fetched from API):
- `useProducts` — `GET /api/v1/products` (paginated, filtered)
- `useProduct` — `GET /api/v1/products/:id`
- `useCategories` — `GET /api/v1/categories`
- `useAuditLogs` — `GET /api/v1/audit-logs` (paginated, filtered)
- `useBulkStatus` — `GET /api/v1/products/bulk/:job_id/status`

**Mutations** (data modifications):
- `useCreateProduct` — `POST /api/v1/products`
- `useUpdateProduct` — `PUT /api/v1/products/:id`
- `useDeleteProduct` — `DELETE /api/v1/products/:id`
- `useCreateCategory` — `POST /api/v1/categories`
- `useUpdateCategory` — `PUT /api/v1/categories/:id`
- `useDeleteCategory` — `DELETE /api/v1/categories/:id`
- `useBulkOperation` — `POST /api/v1/products/bulk`

**Cache Strategy**:
- **Stale-while-revalidate**: Product list, category tree (stale after 5 minutes, refetch in background)
- **Cache invalidation**: On mutation success, invalidate related queries (e.g., create product → invalidate product list)
- **Optimistic updates**: Update UI immediately on mutation, rollback on error

#### Form State (React Hook Form + Zod)

- **ProductForm**: `name`, `description`, `price`, `sku`, `category_ids`, `images`, `stock`, `low_stock_threshold`, `status`, `version`
- **CategoryForm**: `name`, `parent_id`, `order`
- **AuditLogFilters**: `start_date`, `end_date`, `user_id`, `action`, `product_id`

**Validation**:
- Client-side: Zod schema validation (required fields, SKU format, price range, image file size/type)
- Server-side: API enforces business rules (SKU uniqueness, category hierarchy depth, role permissions)

### API Integration Layer

#### API Service Structure

**Base Configuration** (`src/services/api.ts`):
```typescript
import axios from 'axios';
import { authStore } from '@/stores/authStore';

const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL, // e.g., https://api.example.com
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// Request interceptor: Inject auth token
api.interceptors.request.use((config) => {
  const token = authStore.getState().token;
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// Response interceptor: Handle errors and token refresh
api.interceptors.response.use(
  (response) => response,
  async (error) => {
    if (error.response?.status === 401) {
      // Token expired, attempt refresh
      const refreshToken = authStore.getState().refreshToken;
      if (refreshToken) {
        try {
          const { data } = await axios.post('/auth/refresh', { refreshToken });
          authStore.getState().setToken(data.access_token);
          // Retry original request
          error.config.headers.Authorization = `Bearer ${data.access_token}`;
          return api.request(error.config);
        } catch (refreshError) {
          // Refresh failed, logout
          authStore.getState().logout();
          window.location.href = '/login';
        }
      }
    }
    return Promise.reject(error);
  }
);

export default api;
```

**Error Handling Strategy**:
- **Network errors**: Show toast notification, retry button
- **Validation errors (400)**: Display inline error messages on form fields
- **Unauthorized (401)**: Attempt token refresh, logout if refresh fails
- **Forbidden (403)**: Show permission denied message
- **Not found (404)**: Show "resource not found" message
- **Conflict (409)**: Show concurrent edit conflict message, prompt to refresh
- **Server errors (500)**: Show generic error message, log to monitoring

**Retry Logic**:
- Retry on network errors (max 3 retries with exponential backoff)
- No retry on 4xx errors (client errors)
- Retry on 5xx errors (server errors, max 3 retries)

#### Endpoints Consumed

| Endpoint | Method | Purpose | Request | Response | Error Codes |
|---|---|---|---|---|---|
| `/auth/login` | POST | Admin login | `{ email, password }` | `{ access_token, refresh_token, user }` | 401 (invalid credentials) |
| `/auth/refresh` | POST | Refresh access token | `{ refresh_token }` | `{ access_token }` | 401 (invalid refresh token) |
| `/auth/mfa/enable` | POST | Enable MFA | `{ user_id }` | `{ qr_code, secret }` | 403 (permission denied) |
| `/api/v1/products` | GET | List products | Query: `page, limit, search, category_id, min_price, max_price, status` | `{ products: [], total, page, limit }` | 400 (invalid query params) |
| `/api/v1/products/:id` | GET | Get product details | Path: `id` | `{ product }` | 404 (not found) |
| `/api/v1/products` | POST | Create product | `{ name, description, price, sku, category_ids, images, status, stock }` | `{ product }` | 400 (validation error), 409 (SKU duplicate) |
| `/api/v1/products/:id` | PUT | Update product | `{ name, description, price, sku, category_ids, images, status, stock, version }` | `{ product }` | 400 (validation error), 404 (not found), 409 (concurrent edit conflict) |
| `/api/v1/products/:id` | DELETE | Delete product | Query: `hard_delete=true/false` | `{ success: true }` | 403 (permission denied), 404 (not found) |
| `/api/v1/products/bulk` | POST | Bulk operation | `{ product_ids: [], action, params }` | `{ job_id }` | 400 (validation error), 403 (permission denied) |
| `/api/v1/products/bulk/:job_id/status` | GET | Get bulk operation status | Path: `job_id` | `{ status, progress }` | 404 (job not found) |
| `/api/v1/categories` | GET | List categories | None | `{ categories: [] }` | None |
| `/api/v1/categories` | POST | Create category | `{ name, parent_id, order }` | `{ category }` | 400 (validation error), 409 (duplicate name) |
| `/api/v1/categories/:id` | PUT | Update category | `{ name, parent_id, order }` | `{ category }` | 400 (validation error), 404 (not found), 409 (duplicate name) |
| `/api/v1/categories/:id` | DELETE | Delete category | Path: `id` | `{ success: true }` | 403 (permission denied), 404 (not found) |
| `/api/v1/audit-logs` | GET | List audit logs | Query: `page, limit, user_id, action, start_date, end_date, product_id` | `{ logs: [], total, page, limit }` | 403 (permission denied) |
| `/api/v1/audit-logs/export` | POST | Export audit logs | `{ start_date, end_date, format }` | `{ download_url }` | 403 (permission denied) |
| `/api/v1/images/presigned-url` | POST | Get presigned URL for image upload | `{ filename, content_type }` | `{ presigned_url, image_id }` | 400 (invalid file type) |
| `/api/v1/users/me` | GET | Get current user profile | None | `{ user }` | 401 (unauthorized) |
| `/api/v1/users/me` | PUT | Update current user profile | `{ name, email, preferences }` | `{ user }` | 400 (validation error) |

### Routing Design

| Route | Page | Auth Required | Lazy Loaded | Permission |
|---|---|---|---|---|
| `/login` | LoginPage | No | No | Public |
| `/` | DashboardPage | Yes | No | Editor, Manager, Admin |
| `/products/new` | ProductCreatePage | Yes | Yes | Editor, Manager, Admin |
| `/products/:id` | ProductDetailPage | Yes | Yes | Editor, Manager, Admin |
| `/products/:id/edit` | ProductEditPage | Yes | Yes | Editor, Manager, Admin |
| `/categories` | CategoriesPage | Yes | Yes | Editor, Manager, Admin |
| `/audit-logs` | AuditLogsPage | Yes | Yes | Admin |
| `/settings` | SettingsPage | Yes | Yes | All authenticated users |

**Lazy Loading Strategy**:
- Dashboard and Login pages loaded immediately (critical path)
- All other pages lazy-loaded with React.lazy() and Suspense
- Code splitting by route reduces initial bundle size

**Protected Route Implementation**:
```typescript
const ProtectedRoute = ({ children, requiredRole }: { children: React.ReactNode, requiredRole?: string }) => {
  const { isAuthenticated, roles } = authStore();
  
  if (!isAuthenticated) {
    return <Navigate to="/login" />;
  }
  
  if (requiredRole && !roles.includes(requiredRole)) {
    return <PermissionDenied />;
  }
  
  return <>{children}</>;
};
```

### Project Structure

```
src/
├── app/                          # App entry, providers, router
│   ├── App.tsx
│   ├── router.tsx
│   └── providers.tsx
├── features/                     # Feature modules (domain-driven)
│   ├── auth/
│   ├── products/
│   ├── categories/
│   ├── audit-logs/
│   └── bulk-operations/
├── components/                   # Shared UI components
│   ├── ui/                       # Atomic components
│   ├── layout/                   # Layout components
│   └── forms/                    # Form components
├── hooks/                        # Shared custom hooks
├── services/                     # API client and interceptors
├── stores/                       # Zustand stores
├── types/                        # Shared TypeScript types
├── utils/                        # Utility functions
└── styles/                       # Global styles, Tailwind config
```

### Design Decisions & Trade-offs

**Frontend Decisions**:

1. **React over Vue/Angular**: React has the largest ecosystem, best TypeScript support, and most AWS Amplify integrations.
2. **Vite over Create React App**: Vite offers faster dev server, optimized builds, and better DX.
3. **Zustand over Redux Toolkit**: Zustand is simpler, less boilerplate, and sufficient for this app's state complexity.
4. **TanStack Query over manual fetch**: Automatic caching, refetching, and optimistic updates reduce boilerplate and improve UX.
5. **Tailwind CSS over CSS-in-JS**: Tailwind is faster, smaller bundle size, and easier to maintain for utility-first design.
6. **React Hook Form over Formik**: Better performance (uncontrolled inputs), smaller bundle, TypeScript-first.
7. **Radix UI over Material-UI**: Unstyled primitives allow custom design, better accessibility, smaller bundle.

**Trade-offs**:
- **Client-side validation only**: Faster UX, but server-side validation is still enforced for security.
- **Optimistic updates**: Better perceived performance, but requires rollback logic on error.
- **Lazy loading**: Reduces initial bundle size, but adds slight delay when navigating to new pages.

---

## Backend API Design

### Service Scope

**Responsibilities**:
- Authenticate and authorize admin users via AWS Cognito
- Enforce role-based access control (RBAC) for all operations
- Validate and persist product, category, and audit log data
- Generate presigned URLs for secure image uploads to S3
- Process bulk operations asynchronously via SQS + Lambda
- Trigger background jobs for image processing, malware scanning, alerts, and exports
- Provide REST API for web UI and public storefront consumption

**Non-Goals**:
- Public-facing storefront UI (separate application)
- Customer-facing features (reviews, ratings, wishlists)
- Payment processing or order management
- Real-time collaboration features (live cursors, chat)
- Advanced analytics or BI dashboards

**External Dependencies**:
- AWS Cognito (authentication, MFA, user management)
- AWS RDS PostgreSQL (product, category, user data)
- AWS S3 (product images, exports)
- AWS CloudFront (CDN for images)
- AWS DynamoDB (audit logs, bulk operation job status)
- AWS SQS (bulk operation job queue)
- AWS Lambda (image processing, malware scanning, alerts, exports)
- AWS EventBridge (scheduled jobs)

### API Surface

| Endpoint | Method | Purpose | Auth | Request | Response | Notes |
|---|---|---|---|---|---|---|
| `POST /auth/login` | POST | Admin login | Public | `{ email, password }` | `{ access_token, refresh_token, user }` | Cognito integration |
| `POST /auth/refresh` | POST | Refresh access token | Public | `{ refresh_token }` | `{ access_token }` | Cognito integration |
| `POST /auth/mfa/enable` | POST | Enable MFA | Authenticated | `{ user_id }` | `{ qr_code, secret }` | Cognito integration |
| `GET /api/v1/products` | GET | List products (paginated, filtered) | Editor+ | Query: `page, limit, search, category_id, min_price, max_price, status` | `{ products: [], total, page, limit }` | Full-text search, caching |
| `GET /api/v1/products/:id` | GET | Get product details | Editor+ | Path: `id` | `{ product }` | Includes images, categories |
| `POST /api/v1/products` | POST | Create product | Editor+ | `{ name, description, price, sku, category_ids, images, status, stock }` | `{ product }` | Validates SKU uniqueness |
| `PUT /api/v1/products/:id` | PUT | Update product | Editor+ | `{ name, description, price, sku, category_ids, images, status, stock, version }` | `{ product }` | Optimistic locking (version) |
| `DELETE /api/v1/products/:id` | DELETE | Soft-delete or hard-delete product | Manager+ (soft), Admin (hard) | Query: `hard_delete=true/false` | `{ success: true }` | Audit logged |
| `POST /api/v1/products/bulk` | POST | Bulk operation on products | Manager+ | `{ product_ids: [], action, params }` | `{ job_id }` | Enqueues SQS job |
| `GET /api/v1/products/bulk/:job_id/status` | GET | Get bulk operation status | Manager+ | Path: `job_id` | `{ status, progress }` | Polls DynamoDB |
| `GET /api/v1/categories` | GET | List categories (hierarchical) | Editor+ | None | `{ categories: [] }` | Nested tree structure |
| `POST /api/v1/categories` | POST | Create category | Editor+ | `{ name, parent_id, order }` | `{ category }` | Validates name uniqueness per parent |
| `PUT /api/v1/categories/:id` | PUT | Update category | Editor+ | `{ name, parent_id, order }` | `{ category }` | Validates hierarchy depth |
| `DELETE /api/v1/categories/:id` | DELETE | Delete category | Manager+ | Path: `id` | `{ success: true }` | Cascades to product-category assignments |
| `GET /api/v1/audit-logs` | GET | List audit logs (paginated, filtered) | Admin | Query: `page, limit, user_id, action, start_date, end_date, product_id` | `{ logs: [], total, page, limit }` | DynamoDB query |
| `POST /api/v1/audit-logs/export` | POST | Export audit logs to CSV | Admin | `{ start_date, end_date, format }` | `{ download_url }` | Generates S3 presigned URL |
| `POST /api/v1/images/presigned-url` | POST | Get presigned URL for image upload | Editor+ | `{ filename, content_type }` | `{ presigned_url, image_id }` | S3 presigned URL (5 min expiry) |
| `GET /api/v1/users/me` | GET | Get current user profile | Authenticated | None | `{ user }` | Cognito user attributes |
| `PUT /api/v1/users/me` | PUT | Update current user profile | Authenticated | `{ name, email, preferences }` | `{ user }` | Updates Cognito user attributes |

### Application Layers

**Transport / Controllers** (`src/controllers/`):
- `AuthController` — `/auth/*` routes (Cognito integration)
- `ProductController` — `/api/v1/products/*` routes
- `CategoryController` — `/api/v1/categories/*` routes
- `AuditLogController` — `/api/v1/audit-logs/*` routes
- `ImageController` — `/api/v1/images/*` routes
- `UserController` — `/api/v1/users/*` routes

**Service Layer** (`src/services/`):
- `ProductService` — Business logic for product CRUD, search, validation
- `CategoryService` — Business logic for category hierarchy, reordering
- `AuditLogService` — Append-only logging to DynamoDB
- `ImageService` — Presigned URL generation, S3 upload validation
- `BulkOperationService` — Enqueue SQS jobs, track progress in DynamoDB
- `AuthService` — Cognito integration (login, MFA, token refresh)

**Data Access Layer** (`src/repositories/`):
- `ProductRepository` — Prisma ORM for products table
- `CategoryRepository` — Prisma ORM for categories table
- `ProductCategoryRepository` — Prisma ORM for product_categories join table
- `ProductImageRepository` — Prisma ORM for product_images table
- `AuditLogRepository` — DynamoDB SDK for audit_logs table
- `BulkOperationJobRepository` — DynamoDB SDK for bulk_operation_jobs table

**Background Processing** (`src/jobs/`):
- `ImageProcessingJob` — Lambda function triggered by S3 upload event
- `MalwareScanningJob` — Lambda function triggered by S3 upload event
- `LowStockAlertJob` — Lambda function triggered by EventBridge schedule
- `BulkOperationWorker` — Lambda function triggered by SQS message
- `CatalogExportJob` — Lambda function triggered by API request or EventBridge schedule

### Cross-Cutting Design

#### Authentication and Authorization

**Authentication** (AWS Cognito):
- Cognito user pool stores admin users (email, password, MFA settings)
- Login flow: `POST /auth/login` → Cognito `InitiateAuth` → returns JWT access token and refresh token
- MFA flow: If MFA enabled, Cognito returns `MFA_REQUIRED` challenge → user submits MFA code → Cognito returns tokens
- Token refresh: `POST /auth/refresh` → Cognito `InitiateAuth` with refresh token → returns new access token
- Token expiry: Access tokens expire after 30 minutes; refresh tokens valid for 7 days

**Authorization** (RBAC):
- Cognito groups: `editors`, `managers`, `admins`
- API Gateway authorizer validates Cognito JWT tokens
- Lambda functions check user role (from Cognito groups claim) and enforce permissions server-side
- Permission matrix:

| Action | Editor | Manager | Admin |
|---|---|---|---|
| View products | ✅ | ✅ | ✅ |
| Create product | ✅ | ✅ | ✅ |
| Edit product | ✅ | ✅ | ✅ |
| Soft-delete product | ❌ | ✅ | ✅ |
| Hard-delete product | ❌ | ❌ | ✅ |
| Bulk operations | ❌ | ✅ | ✅ |
| View audit logs | ❌ | ❌ | ✅ |
| Manage users | ❌ | ❌ | ✅ |

**Implementation**:
```typescript
// Middleware to check user role
const requireRole = (allowedRoles: string[]) => {
  return (req: Request, res: Response, next: NextFunction) => {
    const userRoles = req.user.roles; // From Cognito JWT claims
    const hasPermission = allowedRoles.some(role => userRoles.includes(role));
    if (!hasPermission) {
      return res.status(403).json({ error: { code: 'FORBIDDEN', message: 'Permission denied' } });
    }
    next();
  };
};

// Usage
router.delete('/api/v1/products/:id', requireRole(['admin']), productController.hardDelete);
```

#### Error Model

**Consistent Error Response Format**:
```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "SKU already exists",
    "details": {
      "field": "sku",
      "value": "ABC123"
    }
  }
}
```

**Error Codes**:
- `VALIDATION_ERROR` (400) — Invalid request payload
- `UNAUTHORIZED` (401) — Missing or invalid auth token
- `FORBIDDEN` (403) — Insufficient permissions
- `NOT_FOUND` (404) — Resource not found
- `CONFLICT` (409) — Concurrent edit conflict, SKU duplicate
- `INTERNAL_SERVER_ERROR` (500) — Unexpected server error

**Error Handling Strategy**:
- All errors logged to CloudWatch with request ID, user ID, and stack trace
- 4xx errors return user-friendly messages
- 5xx errors return generic message ("An unexpected error occurred") and log detailed error for debugging
- Validation errors include field-level details for inline error messages in UI

#### Rate Limiting and Abuse Protection

**API Gateway Throttling**:
- 100 requests per minute per user (based on Cognito user ID)
- Burst limit: 200 requests (allows short bursts)
- Throttled requests return 429 Too Many Requests

**SQS FIFO Queues**:
- Bulk operations use FIFO queues to prevent duplicate processing
- Message deduplication ID based on `job_id`
- Message group ID based on `user_id` (ensures ordered processing per user)

**AWS WAF**:
- DDoS protection via rate-based rules
- Block requests from known malicious IPs
- Geo-blocking (if required by compliance)

#### Logging, Tracing, and Metrics

**CloudWatch Logs**:
- Application logs (info, warn, error) with structured JSON format
- Request/response logs (API Gateway access logs)
- Lambda function logs (invocation, duration, errors)

**AWS X-Ray**:
- Distributed tracing for API requests across API Gateway, Lambda, RDS, DynamoDB, S3
- Trace ID propagated via `X-Amzn-Trace-Id` header
- Service map visualizes dependencies and latency

**CloudWatch Metrics**:
- **API Metrics**: Request count, latency (p50, p95, p99), error rate (4xx, 5xx)
- **Business Metrics**: Products created, bulk operations completed, low-stock alerts sent
- **Infrastructure Metrics**: Lambda invocations, RDS connections, S3 upload size, DynamoDB read/write capacity

**CloudWatch Alarms**:
- API error rate > 5% for 5 minutes → SNS alert to on-call
- RDS CPU > 80% for 10 minutes → SNS alert to DevOps
- Lambda errors > 10 in 5 minutes → SNS alert to on-call
- SQS DLQ message count > 0 → SNS alert to on-call

### Deployment Considerations

**Statelessness**:
- API is stateless; all session state stored in Cognito JWT tokens
- No in-memory session storage; allows horizontal scaling

**Configuration and Secrets**:
- Environment variables for non-sensitive config (API Gateway URL, S3 bucket name, RDS endpoint)
- AWS Secrets Manager for sensitive config (RDS password, Cognito client secret, API keys)
- Secrets rotated automatically via Secrets Manager rotation Lambda

**Horizontal Scaling and Performance Hotspots**:
- **Lambda**: Auto-scales to handle concurrent requests; monitor cold start latency
- **ECS Fargate** (if used): Auto-scaling based on CPU/memory utilization
- **RDS**: Use read replicas for read-heavy queries (product list, search)
- **ElastiCache**: Cache product list, category tree, search results to reduce RDS load
- **CloudFront**: Cache product images at edge locations to reduce S3 load

**Performance Hotspots**:
- Product list query with 100k+ products → Use pagination, indexing, caching
- Full-text search → Use PostgreSQL `tsvector` with GIN index or ElastiCache
- Bulk operations → Async processing via SQS + Lambda to avoid blocking API

### Open Questions

1. **Architecture Choice**: Lambda (serverless) or ECS Fargate (containerized)? **Recommendation**: Lambda for MVP (simpler, auto-scaling, pay-per-use); migrate to Fargate if cold start latency becomes an issue.
2. **Search Strategy**: PostgreSQL full-text search or ElastiCache? **Recommendation**: Start with PostgreSQL; add ElastiCache if search latency > 500ms.
3. **Image Processing Library**: Sharp (Node.js) or Pillow (Python)? **Recommendation**: Sharp (faster, smaller bundle, better TypeScript support).
4. **Bulk Operation Batch Size**: Process 100 products per batch or 1000? **Recommendation**: 100 per batch (safer, easier to retry on failure).

---

## Database Design

### Data Model Overview

**Core Domains**:
- **Product Management**: Products, categories, product-category relationships, product images
- **User Management**: Admin users, roles, permissions (stored in Cognito and RDS)
- **Audit Trail**: Immutable logs of all administrative actions (stored in DynamoDB)
- **Background Jobs**: Bulk operation job status, progress tracking (stored in DynamoDB)

**Primary Ownership Boundaries**:
- Products owned by creator (user_id)
- Categories owned by creator (user_id)
- Audit logs owned by user who performed action (user_id)
- Bulk operation jobs owned by user who initiated job (user_id)

### Entities and Relationships

| Entity | Purpose | Primary Key | Relationships |
|---|---|---|---|
| `products` | Store product data (name, description, price, SKU, stock, status) | `id` (UUID) | Belongs to multiple categories (many-to-many via `product_categories`); has many images (one-to-many via `product_images`); owned by user (foreign key `created_by`) |
| `categories` | Store category hierarchy (name, parent, order) | `id` (UUID) | Self-referential (parent-child via `parent_id`); has many products (many-to-many via `product_categories`) |
| `product_categories` | Join table for product-category many-to-many relationship | `(product_id, category_id)` | Belongs to product and category |
| `product_images` | Store product image metadata (URL, order) | `id` (UUID) | Belongs to product (foreign key `product_id`) |
| `users` | Store admin user metadata (name, email, role) | `id` (UUID) | Owns products, categories, audit logs; synced from Cognito |
| `audit_logs` (DynamoDB) | Immutable log of all administrative actions | `id` (UUID) | References user, product, category, or other resource |
| `bulk_operation_jobs` (DynamoDB) | Track bulk operation job status and progress | `job_id` (UUID) | Belongs to user; references products |

### Schema Notes

#### `products` Table

```sql
CREATE TABLE products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(255) NOT NULL,
  description TEXT,
  price DECIMAL(10, 2) NOT NULL CHECK (price >= 0),
  sku VARCHAR(100) NOT NULL UNIQUE,
  status VARCHAR(20) NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'published')),
  stock INTEGER NOT NULL DEFAULT 0 CHECK (stock >= 0),
  low_stock_threshold INTEGER DEFAULT 10 CHECK (low_stock_threshold >= 0),
  attributes JSONB, -- Custom product attributes (e.g., { "color": "red", "size": "M" })
  version INTEGER NOT NULL DEFAULT 1, -- Optimistic locking
  created_by UUID NOT NULL REFERENCES users(id),
  created_at TIMESTAMP NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW(),
  deleted_at TIMESTAMP -- Soft delete
);

CREATE INDEX idx_products_sku ON products(sku);
CREATE INDEX idx_products_status ON products(status) WHERE deleted_at IS NULL;
CREATE INDEX idx_products_created_at ON products(created_at DESC);
CREATE INDEX idx_products_stock ON products(stock) WHERE stock < low_stock_threshold;
CREATE INDEX idx_products_search ON products USING GIN(to_tsvector('english', name || ' ' || COALESCE(description, '')));
```

**Important Columns**:
- `sku`: Unique product identifier (enforced by unique constraint)
- `status`: Draft or published (controls visibility in public catalog)
- `stock`: Current inventory level
- `low_stock_threshold`: Triggers low-stock alert when stock falls below this value
- `attributes`: JSONB column for custom product attributes (e.g., color, size, material)
- `version`: Optimistic locking to prevent concurrent edit conflicts
- `deleted_at`: Soft delete timestamp (NULL if not deleted)

#### `categories` Table

```sql
CREATE TABLE categories (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(255) NOT NULL,
  parent_id UUID REFERENCES categories(id) ON DELETE CASCADE,
  order INTEGER NOT NULL DEFAULT 0, -- Display order within parent
  created_by UUID NOT NULL REFERENCES users(id),
  created_at TIMESTAMP NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW(),
  UNIQUE(name, parent_id) -- Category name unique per parent
);

CREATE INDEX idx_categories_parent_id ON categories(parent_id);
CREATE INDEX idx_categories_order ON categories(parent_id, order);
```

**Important Columns**:
- `parent_id`: Self-referential foreign key for hierarchical structure (NULL for root categories)
- `order`: Display order within parent category (used for drag-and-drop reordering)
- `UNIQUE(name, parent_id)`: Ensures category name is unique per parent (allows same name in different parents)

#### `product_categories` Table

```sql
CREATE TABLE product_categories (
  product_id UUID NOT NULL REFERENCES products(id) ON DELETE CASCADE,
  category_id UUID NOT NULL REFERENCES categories(id) ON DELETE CASCADE,
  created_at TIMESTAMP NOT NULL DEFAULT NOW(),
  PRIMARY KEY (product_id, category_id)
);

CREATE INDEX idx_product_categories_category_id ON product_categories(category_id);
```

**Important Columns**:
- `(product_id, category_id)`: Composite primary key for many-to-many relationship
- `ON DELETE CASCADE`: Automatically delete join table rows when product or category is deleted

#### `product_images` Table

```sql
CREATE TABLE product_images (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  product_id UUID NOT NULL REFERENCES products(id) ON DELETE CASCADE,
  image_url VARCHAR(500) NOT NULL, -- S3 URL (e.g., https://cdn.example.com/images/abc123.jpg)
  order INTEGER NOT NULL DEFAULT 0, -- Display order
  created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_product_images_product_id ON product_images(product_id, order);
```

**Important Columns**:
- `image_url`: CloudFront URL for product image (original and resized versions stored in S3)
- `order`: Display order for image gallery (used for drag-and-drop reordering)

#### `users` Table

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY, -- Synced from Cognito user ID
  email VARCHAR(255) NOT NULL UNIQUE,
  name VARCHAR(255),
  role VARCHAR(20) NOT NULL CHECK (role IN ('editor', 'manager', 'admin')),
  mfa_enabled BOOLEAN NOT NULL DEFAULT FALSE,
  preferences JSONB, -- User preferences (e.g., { "theme": "dark", "notifications": true })
  created_at TIMESTAMP NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_users_email ON users(email);
```

**Important Columns**:
- `id`: Synced from Cognito user ID (UUID)
- `role`: Editor, manager, or admin (synced from Cognito groups)
- `mfa_enabled`: Whether MFA is enabled for this user (synced from Cognito)
- `preferences`: JSONB column for user preferences (theme, notifications, etc.)

#### `audit_logs` Table (DynamoDB)

```json
{
  "id": "UUID",
  "user_id": "UUID",
  "user_email": "admin@example.com",
  "action": "CREATE_PRODUCT | UPDATE_PRODUCT | DELETE_PRODUCT | CREATE_CATEGORY | UPDATE_CATEGORY | DELETE_CATEGORY",
  "resource_type": "product | category | user",
  "resource_id": "UUID",
  "changes": {
    "before": { "name": "Old Name", "price": 10.00 },
    "after": { "name": "New Name", "price": 15.00 }
  },
  "timestamp": "2025-01-15T12:34:56Z",
  "ip_address": "192.168.1.1",
  "user_agent": "Mozilla/5.0..."
}
```

**DynamoDB Schema**:
- **Partition Key**: `id` (UUID)
- **Sort Key**: `timestamp` (ISO 8601 string)
- **GSI**: `user_id-timestamp-index` (query logs by user)
- **GSI**: `resource_id-timestamp-index` (query logs by resource)
- **TTL**: `ttl` (2 years from timestamp; auto-delete after expiry)

**Important Attributes**:
- `changes`: JSON object with `before` and `after` snapshots of changed fields
- `timestamp`: ISO 8601 timestamp for sorting and filtering
- `ip_address` and `user_agent`: For security auditing

#### `bulk_operation_jobs` Table (DynamoDB)

```json
{
  "job_id": "UUID",
  "user_id": "UUID",
  "action": "edit | delete | categorize",
  "product_ids": ["UUID1", "UUID2", "..."],
  "params": { "category_ids": ["UUID3"], "status": "published" },
  "status": "pending | processing | completed | failed",
  "progress": 0-100,
  "total_items": 100,
  "processed_items": 50,
  "failed_items": 2,
  "error_message": "SKU duplicate for product ABC123",
  "created_at": "2025-01-15T12:34:56Z",
  "updated_at": "2025-01-15T12:35:56Z"
}
```

**DynamoDB Schema**:
- **Partition Key**: `job_id` (UUID)
- **GSI**: `user_id-created_at-index` (query jobs by user)
- **TTL**: `ttl` (30 days from created_at; auto-delete after expiry)

**Important Attributes**:
- `status`: Pending, processing, completed, or failed
- `progress`: Percentage (0-100) for progress indicator in UI
- `failed_items`: Count of items that failed processing (for retry logic)

### Query and Index Strategy

**High-Frequency Reads**:
- Product list (paginated, filtered by category, price, status, search query)
- Category tree (hierarchical, ordered)
- Audit logs (paginated, filtered by user, action, date range)
- Bulk operation job status (polled every 2 seconds during processing)

**Write-Heavy Paths**:
- Product creation, editing, deletion (CRUD operations)
- Audit log appends (every administrative action)
- Bulk operation job status updates (every batch processed)

**Suggested Indexes**:

| Table | Index | Purpose | Type |
|---|---|---|---|
| `products` | `idx_products_sku` | Enforce SKU uniqueness, fast lookup by SKU | B-tree |
| `products` | `idx_products_status` | Filter by status (draft/published), exclude soft-deleted | Partial B-tree |
| `products` | `idx_products_created_at` | Sort by creation date (newest first) | B-tree (DESC) |
| `products` | `idx_products_stock` | Find low-stock products for alerts | Partial B-tree |
| `products` | `idx_products_search` | Full-text search on name and description | GIN (tsvector) |
| `categories` | `idx_categories_parent_id` | Query child categories by parent | B-tree |
| `categories` | `idx_categories_order` | Sort categories by order within parent | B-tree |
| `product_categories` | `idx_product_categories_category_id` | Query products by category | B-tree |
| `product_images` | `idx_product_images_product_id` | Query images by product, ordered | B-tree |
| `users` | `idx_users_email` | Fast lookup by email (login) | B-tree |

**Query Optimization**:
- Use `LIMIT` and `OFFSET` for pagination (or cursor-based pagination for large datasets)
- Use `WHERE deleted_at IS NULL` to exclude soft-deleted products
- Use `to_tsvector` and `to_tsquery` for full-text search (PostgreSQL)
- Use `EXPLAIN ANALYZE` to identify slow queries and missing indexes

### Migration Strategy

**Initial Schema Rollout**:
- Use Prisma Migrate or Flyway for schema versioning
- Initial migration creates all tables, indexes, and constraints
- Seed database with default admin user (synced from Cognito)

**Future Evolution Approach**:
- All schema changes versioned via migrations (no manual SQL)
- Backward-compatible changes (add column, add index) applied immediately
- Breaking changes (rename column, drop table) require multi-step migration:
  1. Add new column/table alongside old
  2. Backfill data
  3. Update application code to use new column/table
  4. Drop old column/table in next release

**Backfill and Compatibility Concerns**:
- Custom product attributes (JSONB column) added in later migration; existing products have `NULL` attributes
- Soft-delete column (`deleted_at`) added in later migration; existing products have `NULL` (not deleted)
- Optimistic locking (`version` column) added in later migration; existing products have `version = 1`

### Reliability and Governance

**Backups and Restore Expectations**:
- **RDS**: Automated daily backups (7-day retention); point-in-time recovery within 7 days
- **S3**: Versioning enabled for product images; lifecycle policy transitions old versions to Glacier after 90 days
- **DynamoDB**: Point-in-time recovery enabled for audit logs and bulk operation jobs (35-day retention)

**Retention Requirements**:
- **Products**: Retained indefinitely (unless hard-deleted by admin)
- **Soft-deleted products**: Retained for 90 days, then hard-deleted via scheduled job
- **Audit logs**: Retained for 2 years in DynamoDB; archived to S3 after 1 year for cost optimization
- **Bulk operation jobs**: Retained for 30 days in DynamoDB; auto-deleted via TTL

**Access Controls and Sensitive Data Handling**:
- **PII**: User email and name stored in `users` table; encrypted at rest (AES-256)
- **Audit logs**: May contain PII in `changes` field; access restricted to admin role
- **Database access**: RDS accessible only from API Lambda/Fargate (no public access); IAM authentication for RDS connections
- **Secrets**: RDS password stored in AWS Secrets Manager; rotated automatically every 90 days

### Open Questions

1. **Category Hierarchy Depth Limit**: Should we enforce a maximum depth (e.g., 5 levels) via database constraint or application logic? **Recommendation**: Application logic (more flexible).
2. **Product Attributes Schema**: Should we use JSONB column or EAV (Entity-Attribute-Value) model? **Recommendation**: JSONB (simpler, better performance for read-heavy queries).
3. **Audit Log Archival**: Should we archive audit logs to S3 after 1 year or keep in DynamoDB? **Recommendation**: Archive to S3 for cost optimization; DynamoDB on-demand pricing is expensive for long-term storage.
4. **Soft-Delete Retention**: Should soft-deleted products be retained for 90 days or indefinitely? **Recommendation**: 90 days (balances recoverability with storage cost).

---

## AWS Infrastructure Design

### Compute Services

**API Backend**:
- **Option 1: AWS Lambda** (Recommended for MVP)
  - Serverless, auto-scaling, pay-per-use
  - Node.js 20 runtime with Express.js
  - Provisioned concurrency for low latency (optional)
  - Cold start mitigation: Keep functions warm via EventBridge scheduled ping
- **Option 2: ECS Fargate** (If cold start latency is unacceptable)
  - Containerized Express.js app
  - Auto-scaling based on CPU/memory utilization
  - ALB for load balancing
  - Higher cost but predictable latency

**Background Jobs**:
- **AWS Lambda** for all background jobs:
  - Image processing (triggered by S3 upload event)
  - Malware scanning (triggered by S3 upload event)
  - Low-stock alerts (triggered by EventBridge schedule)
  - Bulk operation workers (triggered by SQS message)
  - Catalog export (triggered by API request or EventBridge schedule)

### Storage Services

**Object Storage**:
- **Amazon S3**:
  - Bucket for product images (`product-images-{env}`)
  - Bucket for catalog exports (`catalog-exports-{env}`)
  - Bucket for audit log archives (`audit-logs-archive-{env}`)
  - Versioning enabled for images (lifecycle policy transitions old versions to Glacier after 90 days)
  - Encryption at rest (AES-256, SSE-S3 or SSE-KMS)
  - Public access blocked; images served via CloudFront

**CDN**:
- **Amazon CloudFront**:
  - Distribution for product images (origin: S3 bucket)
  - Cache TTL: 1 year for images (versioned URLs for cache invalidation)
  - HTTPS only (TLS 1.2+)
  - Custom domain (e.g., `cdn.example.com`)

**Database**:
- **Amazon RDS PostgreSQL 15**:
  - Multi-AZ deployment for high availability
  - Instance type: `db.t4g.medium` (2 vCPU, 4 GB RAM) for MVP; scale up as needed
  - Storage: 100 GB GP3 SSD (auto-scaling enabled)
  - Automated backups (7-day retention)
  - Encryption at rest (AES-256, KMS)
  - Read replicas (optional, for read-heavy queries)

**NoSQL Database**:
- **Amazon DynamoDB**:
  - Table for audit logs (`audit_logs`)
  - Table for bulk operation jobs (`bulk_operation_jobs`)
  - On-demand billing (pay-per-request)
  - Point-in-time recovery enabled
  - Encryption at rest (AES-256, KMS)
  - TTL enabled for auto-deletion (2 years for audit logs, 30 days for bulk jobs)

**Cache** (Optional):
- **Amazon ElastiCache (Redis)**:
  - Cache product list, category tree, search results
  - Instance type: `cache.t4g.micro` (2 vCPU, 0.5 GB RAM) for MVP
  - Cluster mode disabled (single node for MVP)
  - Encryption in transit (TLS) and at rest (AES-256)

### Messaging Services

**Queue**:
- **Amazon SQS**:
  - FIFO queue for bulk operations (`bulk-operations.fifo`)
  - Message retention: 4 days
  - Visibility timeout: 5 minutes (time for Lambda to process one batch)
  - Dead-letter queue (DLQ) for failed messages
  - Encryption at rest (AES-256, KMS)

**Notifications**:
- **Amazon SNS**:
  - Topic for low-stock alerts (`low-stock-alerts`)
  - Topic for CloudWatch alarms (`cloudwatch-alarms`)
  - Email subscriptions for admins and on-call engineers

### API Gateway

**Amazon API Gateway (REST API)**:
- Single API Gateway for all backend endpoints (`/auth/*`, `/api/v1/*`)
- Custom domain (e.g., `api.example.com`)
- Cognito authorizer for JWT validation
- Throttling: 100 requests per minute per user
- Caching: 5-minute TTL for `GET /api/v1/products` (optional)
- CORS enabled for web UI origin
- Access logs to CloudWatch Logs
- X-Ray tracing enabled

### Authentication

**Amazon Cognito**:
- User pool for admin users (`admin-user-pool`)
- Password policy: Min 8 chars, uppercase, lowercase, number, special char
- MFA: TOTP (Google Authenticator, Authy) or SMS
- Groups: `editors`, `managers`, `admins`
- JWT token expiry: Access token 30 minutes, refresh token 7 days
- Custom attributes: `role`, `preferences`

### Monitoring and Observability

**Amazon CloudWatch**:
- **Logs**: Application logs, API Gateway access logs, Lambda function logs
- **Metrics**: API latency, error rate, Lambda invocations, RDS connections, DynamoDB read/write capacity
- **Dashboards**: Real-time metrics for API, database, background jobs
- **Alarms**: API error rate > 5%, RDS CPU > 80%, Lambda errors > 10, SQS DLQ message count > 0

**AWS X-Ray**:
- Distributed tracing for API requests across API Gateway, Lambda, RDS, DynamoDB, S3
- Service map visualizes dependencies and latency
- Trace ID propagated via `X-Amzn-Trace-Id` header

### Security Services

**AWS WAF**:
- Web ACL attached to API Gateway
- Rate-based rule: Block IP if > 2000 requests in 5 minutes
- Geo-blocking (if required by compliance)
- Managed rule groups: Core rule set, known bad inputs

**AWS Secrets Manager**:
- RDS password (auto-rotated every 90 days)
- Cognito client secret
- Third-party API keys (if any)

**AWS KMS**:
- Customer-managed keys (CMK) for encryption at rest:
  - RDS encryption key
  - S3 encryption key
  - DynamoDB encryption key
  - SQS encryption key
- Key rotation enabled (annual)

**AWS GuardDuty**:
- Threat detection for suspicious activity (e.g., unusual API calls, compromised credentials)
- Findings sent to SNS topic for alerting

### Scheduler

**Amazon EventBridge**:
- Scheduled rules:
  - Low-stock alerts: Every 1 hour
  - Catalog export: Daily at 2 AM (if scheduled exports enabled)
  - Soft-delete cleanup: Daily at 3 AM (hard-delete products with `deleted_at` > 90 days ago)
  - Audit log archival: Monthly (move logs older than 1 year to S3)

### CI/CD Pipeline

**GitHub Actions** (or AWS CodePipeline):
- **Build**: Install dependencies, run tests, build frontend (Vite), build backend (TypeScript)
- **Test**: Unit tests (Vitest, Jest), integration tests (Supertest), code coverage (> 80%)
- **Deploy**:
  - Frontend: Upload to S3, invalidate CloudFront cache
  - Backend: Deploy Lambda function (ZIP or container image) or ECS Fargate task
  - Database: Run Prisma migrations
- **Environments**: `dev`, `staging`, `prod`
- **Rollback**: Blue/green deployment for zero-downtime rollback

### Infrastructure as Code

**AWS CDK (TypeScript)** or **Terraform**:
- Define all infrastructure as code
- Separate stacks for compute, storage, networking, monitoring
- Environment-specific configuration (dev, staging, prod)
- Version control in Git
- CI/CD pipeline applies infrastructure changes

### Network Architecture

**VPC**:
- Public subnets: API Gateway, CloudFront (if needed)
- Private subnets: Lambda, ECS Fargate, RDS, ElastiCache
- NAT Gateway for Lambda/Fargate to access internet (S3, Cognito, etc.)
- Security groups:
  - API Gateway: Allow HTTPS (443) from internet
  - Lambda/Fargate: Allow outbound to RDS, DynamoDB, S3, Cognito
  - RDS: Allow inbound from Lambda/Fargate security group only
  - ElastiCache: Allow inbound from Lambda/Fargate security group only

### Cost Optimization

**Estimated Monthly Cost (MVP, 50 concurrent users, 100k products)**:
- **API Gateway**: $3.50 (1M requests)
- **Lambda**: $20 (1M invocations, 512 MB, 1s avg duration)
- **RDS PostgreSQL**: $50 (db.t4g.medium, Multi-AZ, 100 GB storage)
- **DynamoDB**: $10 (on-demand, 1M writes, 10M reads)
- **S3**: $5 (100 GB storage, 1M requests)
- **CloudFront**: $10 (1 TB data transfer)
- **Cognito**: $0 (free tier covers 50 users)
- **CloudWatch**: $5 (logs, metrics, alarms)
- **Total**: ~$100/month

**Cost Optimization Strategies**:
- Use Lambda instead of ECS Fargate (pay-per-use vs. always-on)
- Use RDS read replicas only if needed (adds $50/month)
- Use ElastiCache only if PostgreSQL search is too slow (adds $15/month)
- Use S3 lifecycle policies to transition old images to Glacier (saves 80% storage cost)
- Use DynamoDB on-demand billing (cheaper for unpredictable load)

---

## Integration & Background Jobs

### Image Processing

**Trigger**: S3 upload event (via presigned URL)

**Responsibility**:
- Resize uploaded image to multiple dimensions (thumbnail: 150x150, medium: 600x600, large: 1200x1200)
- Store resized images in S3 with versioned URLs (e.g., `images/abc123-thumbnail.jpg?v=1`)
- Update `product_images` table with CloudFront URLs

**Implementation**:
- Lambda function (Node.js 20) with Sharp library
- Triggered by S3 `ObjectCreated` event
- Timeout: 5 minutes (sufficient for 50 MB images)
- Memory: 1024 MB (faster processing)

**Failure Handling**:
- Retry 3 times with exponential backoff (1s, 2s, 4s)
- Send failed events to DLQ
- Alert admin via SNS if DLQ message count > 0

**Observability**:
- CloudWatch Logs: Log image size, processing time, errors
- X-Ray tracing: Trace S3 upload → Lambda → S3 resized images
- Custom metric: `ImageProcessingDuration` (p95, p99)

### Malware Scanning

**Trigger**: S3 upload event (via presigned URL)

**Responsibility**:
- Scan uploaded image with ClamAV or AWS-native solution (e.g., Trend Micro File Security)
- Delete infected files from S3
- Log scan result to CloudWatch Logs
- Alert admin via SNS if malware detected

**Implementation**:
- Lambda function (Node.js 20) with ClamAV Lambda layer or third-party scanner
- Triggered by S3 `ObjectCreated` event
- Timeout: 2 minutes
- Memory: 512 MB

**Failure Handling**:
- Retry 3 times with exponential backoff
- Send failed events to DLQ
- Alert admin via SNS if DLQ message count > 0

**Observability**:
- CloudWatch Logs: Log scan result (clean, infected, error)
- Custom metric: `MalwareScanResult` (count of clean, infected, error)

### Low-Stock Alerts

**Trigger**: EventBridge scheduled rule (every 1 hour)

**Responsibility**:
- Query `products` table for products with `stock < low_stock_threshold`
- Send SNS notification to admins with list of low-stock products
- Log alert to CloudWatch Logs

**Implementation**:
- Lambda function (Node.js 20) with Prisma ORM
- Triggered by EventBridge rule (`rate(1 hour)`)
- Timeout: 1 minute
- Memory: 256 MB

**Failure Handling**:
- Retry 3 times with exponential backoff
- Log failure to CloudWatch Logs
- Manual review if alerts not sent

**Observability**:
- CloudWatch Logs: Log low-stock products, alert sent
- Custom metric: `LowStockAlertsSent` (count)

### Bulk Operation Worker

**Trigger**: SQS message (from bulk operation API)

**Responsibility**:
- Process bulk edit/delete/categorize on products in batches (100 products per batch)
- Update products in RDS
- Update job status and progress in DynamoDB
- Log errors to CloudWatch Logs

**Implementation**:
- Lambda function (Node.js 20) with Prisma ORM
- Triggered by SQS FIFO queue
- Timeout: 5 minutes (sufficient for 100 products)
- Memory: 512 MB
- Batch size: 10 messages (process 1000 products per invocation)

**Failure Handling**:
- Retry 3 times with exponential backoff
- Send failed messages to DLQ
- Mark job as `failed` in DynamoDB
- Allow manual retry via API

**Observability**:
- CloudWatch Logs: Log batch processing time, errors
- X-Ray tracing: Trace SQS → Lambda → RDS → DynamoDB
- Custom metric: `BulkOperationJobCompletionTime` (p95, p99)

### Catalog Export

**Trigger**: API request (`POST /api/v1/audit-logs/export`) or EventBridge scheduled rule (daily at 2 AM)

**Responsibility**:
- Query `products` table for all products (or filtered by date range)
- Generate CSV or JSON file
- Upload to S3 (`catalog-exports-{env}` bucket)
- Generate presigned URL for download (valid for 7 days)
- Send SNS email with download link (optional)

**Implementation**:
- Lambda function (Node.js 20) with Prisma ORM and `csv-writer` library
- Triggered by API request or EventBridge rule
- Timeout: 15 minutes (sufficient for 1M products)
- Memory: 1024 MB

**Failure Handling**:
- Retry 3 times with exponential backoff
- Log failure to CloudWatch Logs
- Manual retry via API

**Observability**:
- CloudWatch Logs: Log export size, processing time, errors
- Custom metric: `CatalogExportSize` (bytes), `CatalogExportDuration` (seconds)

---

## Security & Compliance

### Encryption

**In Transit**:
- All API requests use HTTPS (TLS 1.2+)
- API Gateway enforces HTTPS
- CloudFront enforces HTTPS
- RDS connections use TLS
- ElastiCache connections use TLS (if enabled)

**At Rest**:
- RDS: AES-256 encryption (KMS)
- S3: AES-256 encryption (SSE-S3 or SSE-KMS)
- DynamoDB: AES-256 encryption (KMS)
- SQS: AES-256 encryption (KMS)
- Secrets Manager: AES-256 encryption (KMS)

### Role-Based Access Control (RBAC)

**Roles**:
- **Editor**: Can create, edit products and categories; cannot delete or perform bulk operations
- **Manager**: Can create, edit, soft-delete products and categories; can perform bulk operations
- **Admin**: Full access (hard-delete, manage users, view audit logs)

**Enforcement**:
- API Gateway authorizer validates Cognito JWT tokens
- Lambda functions check user role (from Cognito groups claim) and enforce permissions server-side
- No client-side authorization (all permissions checked server-side)

### Audit Logging

**What is Logged**:
- All product CRUD operations (create, update, delete)
- All category CRUD operations (create, update, delete)
- All bulk operations (edit, delete, categorize)
- User login, logout, MFA enable/disable
- User profile updates

**Log Format**:
```json
{
  "id": "UUID",
  "user_id": "UUID",
  "user_email": "admin@example.com",
  "action": "UPDATE_PRODUCT",
  "resource_type": "product",
  "resource_id": "UUID",
  "changes": {
    "before": { "name": "Old Name", "price": 10.00 },
    "after": { "name": "New Name", "price": 15.00 }
  },
  "timestamp": "2025-01-15T12:34:56Z",
  "ip_address": "192.168.1.1",
  "user_agent": "Mozilla/5.0..."
}
```

**Storage**:
- DynamoDB (append-only, immutable)
- Retained for 2 years
- Archived to S3 after 1 year for cost optimization

**Access Control**:
- Only admins can view audit logs
- Audit logs cannot be modified or deleted (immutable)

### Rate Limiting

**API Gateway Throttling**:
- 100 requests per minute per user (based on Cognito user ID)
- Burst limit: 200 requests (allows short bursts)
- Throttled requests return 429 Too Many Requests

**SQS FIFO Queues**:
- Bulk operations use FIFO queues to prevent duplicate processing
- Message deduplication ID based on `job_id`
- Message group ID based on `user_id` (ensures ordered processing per user)

**AWS WAF**:
- Rate-based rule: Block IP if > 2000 requests in 5 minutes
- Managed rule groups: Core rule set, known bad inputs

### Compliance

**WCAG 2.1 Level AA**:
- Web UI designed with semantic HTML, ARIA labels, keyboard navigation, color contrast
- Automated testing with axe, Lighthouse
- Manual screen reader testing

**Data Residency**:
- All AWS resources provisioned in specified region (e.g., us-east-1, eu-west-1)
- No cross-region replication (unless required for disaster recovery)

**PII Handling**:
- User email and name encrypted at rest (AES-256)
- Audit logs may contain PII in `changes` field; access restricted to admins
- No credit card or payment data stored

---

## Observability & Operations

### Logging

**Application Logs**:
- Structured JSON format (timestamp, level, message, context)
- Logged to CloudWatch Logs
- Log levels: DEBUG, INFO, WARN, ERROR
- Retention: 30 days (configurable)

**API Gateway Access Logs**:
- Request ID, method, path, status code, latency, user ID
- Logged to CloudWatch Logs
- Retention: 30 days

**Lambda Function Logs**:
- Invocation ID, duration, memory used, errors
- Logged to CloudWatch Logs
- Retention: 30 days

### Tracing

**AWS X-Ray**:
- Distributed tracing for API requests across API Gateway, Lambda, RDS, DynamoDB, S3
- Trace ID propagated via `X-Amzn-Trace-Id` header
- Service map visualizes dependencies and latency
- Trace retention: 30 days

### Metrics

**CloudWatch Metrics**:
- **API Metrics**: Request count, latency (p50, p95, p99), error rate (4xx, 5xx)
- **Business Metrics**: Products created, bulk operations completed, low-stock alerts sent
- **Infrastructure Metrics**: Lambda invocations, RDS connections, S3 upload size, DynamoDB read/write capacity

**Custom Metrics**:
- `ImageProcessingDuration` (p95, p99)
- `MalwareScanResult` (count of clean, infected, error)
- `LowStockAlertsSent` (count)
- `BulkOperationJobCompletionTime` (p95, p99)
- `CatalogExportSize` (bytes), `CatalogExportDuration` (seconds)

### Alerting

**CloudWatch Alarms**:
- API error rate > 5% for 5 minutes → SNS alert to on-call
- RDS CPU > 80% for 10 minutes → SNS alert to DevOps
- Lambda errors > 10 in 5 minutes → SNS alert to on-call
- SQS DLQ message count > 0 → SNS alert to on-call
- Low-stock alerts not sent for 2 hours → SNS alert to on-call

**SNS Topics**:
- `cloudwatch-alarms` (email subscriptions for on-call engineers)
- `low-stock-alerts` (email subscriptions for admins)

### Backup & Restore

**RDS**:
- Automated daily backups (7-day retention)
- Point-in-time recovery within 7 days
- Manual snapshots before major schema changes

**S3**:
- Versioning enabled for product images
- Lifecycle policy transitions old versions to Glacier after 90 days
- Cross-region replication (optional, for disaster recovery)

**DynamoDB**:
- Point-in-time recovery enabled (35-day retention)
- On-demand backups before major application changes

**Restore Testing**:
- Quarterly restore drills to validate RTO/RPO
- Restore to separate environment (not production)

---

## Deployment Strategy

### CI/CD Pipeline

**GitHub Actions** (or AWS CodePipeline):

**Build Stage**:
- Install dependencies (`npm install`)
- Run linter (`npm run lint`)
- Run unit tests (`npm test`)
- Build frontend (`npm run build` for Vite)
- Build backend (`npm run build` for TypeScript)
- Generate code coverage report (> 80% required)

**Test Stage**:
- Run integration tests (`npm run test:integration`)
- Run E2E tests (Playwright or Cypress)
- Security scan (Snyk, npm audit)

**Deploy Stage**:
- **Frontend**:
  - Upload build artifacts to S3 (`s3://web-ui-{env}`)
  - Invalidate CloudFront cache (`/*`)
- **Backend**:
  - Package Lambda function (ZIP or container image)
  - Deploy to Lambda (`aws lambda update-function-code`)
  - Or deploy to ECS Fargate (update task definition, rolling update)
- **Database**:
  - Run Prisma migrations (`npx prisma migrate deploy`)
- **Infrastructure**:
  - Apply CDK/Terraform changes (`cdk deploy` or `terraform apply`)

**Environments**:
- `dev`: Continuous deployment on every commit to `main`
- `staging`: Manual approval required
- `prod`: Manual approval required, blue/green deployment

### Blue/Green Deployment

**Strategy**:
- Deploy new version to "green" environment (separate Lambda alias or ECS task)
- Run smoke tests on green environment
- Switch traffic from "blue" to "green" (API Gateway stage variable or ALB target group)
- Monitor for errors (5 minutes)
- If errors detected, rollback to "blue" (instant switch)
- If no errors, decommission "blue" environment

**Rollback Plan**:
- Keep previous Lambda version or ECS task definition
- Switch API Gateway stage variable or ALB target group back to previous version
- Rollback database migrations (if backward-compatible)
- Rollback frontend (re-upload previous S3 build, invalidate CloudFront cache)

### Monitoring Playbook

**Post-Deployment Monitoring** (first 24 hours):
- Monitor API error rate (should be < 1%)
- Monitor API latency (p95 should be < 500ms)
- Monitor Lambda errors (should be 0)
- Monitor RDS connections (should be < 50% of max)
- Monitor SQS DLQ message count (should be 0)
- Monitor CloudWatch alarms (should be 0 triggered alarms)

**Incident Response**:
- If API error rate > 5% for 5 minutes → rollback immediately
- If RDS CPU > 90% for 5 minutes → scale up RDS instance
- If Lambda errors > 10 in 5 minutes → investigate logs, rollback if needed
- If SQS DLQ message count > 0 → investigate failed messages, manual retry

---

## Design Decisions & Trade-offs

### Key Design Decisions

1. **Modular Monolith over Microservices**:
   - **Rationale**: Simpler deployment, shared domain logic, easier debugging for MVP
   - **Trade-off**: Harder to scale individual components independently; can extract microservices later if needed

2. **Lambda over ECS Fargate**:
   - **Rationale**: Serverless, auto-scaling, pay-per-use, lower operational complexity
   - **Trade-off**: Cold start latency (mitigated with provisioned concurrency); 15-minute timeout limit

3. **PostgreSQL over DynamoDB for Primary Database**:
   - **Rationale**: Relational model fits product-category hierarchy; ACID transactions; full-text search
   - **Trade-off**: Harder to scale horizontally; requires read replicas for read-heavy queries

4. **DynamoDB for Audit Logs**:
   - **Rationale**: Append-only, immutable, scalable, point-in-time recovery
   - **Trade-off**: More expensive for long-term storage; archived to S3 after 1 year

5. **React over Vue/Angular**:
   - **Rationale**: Largest ecosystem, best TypeScript support, most AWS Amplify integrations
   - **Trade-off**: More boilerplate than Vue; steeper learning curve than Vue

6. **Zustand over Redux Toolkit**:
   - **Rationale**: Simpler, less boilerplate, sufficient for this app's state complexity
   - **Trade-off**: Less mature ecosystem than Redux; fewer DevTools integrations

7. **TanStack Query over Manual Fetch**:
   - **Rationale**: Automatic caching, refetching, optimistic updates reduce boilerplate and improve UX
   - **Trade-off**: Learning curve; adds dependency

8. **Tailwind CSS over CSS-in-JS**:
   - **Rationale**: Faster, smaller bundle size, easier to maintain for utility-first design
   - **Trade-off**: Less dynamic styling; requires build step

9. **Presigned URLs for Image Upload**:
   - **Rationale**: Direct upload to S3 reduces API load; faster upload for large files
   - **Trade-off**: More complex client-side logic; requires S3 event trigger for processing

10. **Optimistic Locking for Concurrent Edits**:
    - **Rationale**: Prevents data loss from concurrent edits; user-friendly conflict resolution
    - **Trade-off**: Requires version field in database; adds complexity to update logic

### Trade-offs

| Decision | Benefit | Cost | Mitigation |
|---|---|---|---|
| Lambda over ECS Fargate | Lower cost, auto-scaling, simpler ops | Cold start latency | Provisioned concurrency for critical endpoints |
| PostgreSQL over DynamoDB | Relational model, ACID, full-text search | Harder to scale horizontally | Use read replicas for read-heavy queries |
| DynamoDB for Audit Logs | Scalable, immutable, point-in-time recovery | Expensive for long-term storage | Archive to S3 after 1 year |
| Presigned URLs for Image Upload | Faster upload, reduced API load | More complex client-side logic | Provide clear error messages, retry logic |
| Optimistic Locking | Prevents data loss from concurrent edits | Adds complexity to update logic | Show user-friendly conflict resolution UI |
| Client-side Validation Only | Faster UX | Security risk if server-side validation missing | Always enforce server-side validation |
| Lazy Loading for Routes | Reduces initial bundle size | Slight delay when navigating to new pages | Prefetch critical routes on hover |
| Soft Delete | Recoverability | Storage cost for deleted products | Hard-delete after 90 days via scheduled job |

---

## Open Questions

1. **Data Residency Region**: Which AWS region should be used for data residency compliance (e.g., us-east-1, eu-west-1, ap-southeast-2)? **Owner**: Product Owner / Legal / Compliance
2. **MFA Enforcement Policy**: Should MFA be required for all admin roles, or only for manager and admin roles? **Owner**: Security / Product Owner
3. **Search Strategy**: Is PostgreSQL full-text search sufficient, or should we plan for OpenSearch/ElastiCache from the start? **Owner**: Engineering / Product Owner
4. **Acceptable Page Load Time**: Is < 2 seconds acceptable for initial load of 100k product list, or should we optimize further (e.g., virtual scrolling, server-side pagination)? **Owner**: Product Owner / UX
5. **Backup Retention Period**: How long should RDS backups and S3 image versions be retained (default 7 days for RDS, indefinite for S3)? **Owner**: Product Owner / Compliance
6. **Audit Log Export Format**: Should audit log exports include all fields (including PII), or should PII be redacted? **Owner**: Product Owner / Legal / Compliance
7. **Scheduled Catalog Export Frequency**: How often should scheduled catalog exports run (daily, weekly, on-demand only)? **Owner**: Product Owner
8. **Budget for Accessibility Audit**: Is there budget for a third-party accessibility audit, or should we rely on automated testing only? **Owner**: Product Owner / Finance
9. **Brand Guidelines and Design System**: Does a brand guideline or design system exist, or should we create one from scratch? **Owner**: Product Owner / Design
10. **Minimum Supported Screen Size**: What is the minimum supported screen size for tablet devices (e.g., 768px, 1024px)? **Owner**: Product Owner / UX

---

**This design is ready for implementation.** All architectural layers (frontend, backend, database, infrastructure) are cohesive and aligned with the confirmed build plan. The design preserves key decisions (AWS-only, RBAC, audit logging, optimistic locking) that should guide downstream task breakdown.

## Diagram

```xml
<mxfile host="app.diagrams.net" agent="Claude" version="24.7.17">
  <diagram id="product-catalog-arch" name="Product Catalog Management System">
    <mxGraphModel dx="1434" dy="759" grid="1" gridSize="10" guides="1" tooltips="1" connect="1" arrows="1" fold="1" page="1" pageScale="1" pageWidth="1169" pageHeight="827" math="0" shadow="0">
      <root>
        <mxCell id="0" />
        <mxCell id="1" parent="0" />
        
        <!-- External Users -->
        <mxCell id="2" value="Admin Users" style="sketch=0;outlineConnect=0;fontColor=#232F3E;fillColor=#232F3E;strokeColor=none;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=12;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.users;" vertex="1" parent="1">
          <mxGeometry x="40" y="340" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- AWS Cloud Container -->
        <mxCell id="3" value="AWS Cloud" style="points=[[0,0],[0.25,0],[0.5,0],[0.75,0],[1,0],[1,0.25],[1,0.5],[1,0.75],[1,1],[0.75,1],[0.5,1],[0.25,1],[0,1],[0,0.75],[0,0.5],[0,0.25]];outlineConnect=0;gradientColor=none;html=1;whiteSpace=wrap;fontSize=12;fontStyle=0;shape=mxgraph.aws4.group;grIcon=mxgraph.aws4.group_aws_cloud_alt;strokeColor=#232F3E;fillColor=none;verticalAlign=top;align=left;spacingLeft=30;fontColor=#232F3E;dashed=0;labelBackgroundColor=none;container=1;pointerEvents=0;collapsible=0;recursiveResize=0;" vertex="1" parent="1">
          <mxGeometry x="160" y="40" width="960" height="720" as="geometry" />
        </mxCell>
        
        <!-- Region Container -->
        <mxCell id="4" value="us-east-1" style="points=[[0,0],[0.25,0],[0.5,0],[0.75,0],[1,0],[1,0.25],[1,0.5],[1,0.75],[1,1],[0.75,1],[0.5,1],[0.25,1],[0,1],[0,0.75],[0,0.5],[0,0.25]];outlineConnect=0;gradientColor=none;html=1;whiteSpace=wrap;fontSize=12;fontStyle=0;shape=mxgraph.aws4.group;grIcon=mxgraph.aws4.group_region;strokeColor=#00A4A6;fillColor=none;verticalAlign=top;align=left;spacingLeft=30;fontColor=#147EBA;dashed=1;labelBackgroundColor=none;container=1;pointerEvents=0;collapsible=0;recursiveResize=0;" vertex="1" parent="3">
          <mxGeometry x="20" y="40" width="920" height="660" as="geometry" />
        </mxCell>
        
        <!-- VPC Container -->
        <mxCell id="5" value="VPC" style="points=[[0,0],[0.25,0],[0.5,0],[0.75,0],[1,0],[1,0.25],[1,0.5],[1,0.75],[1,1],[0.75,1],[0.5,1],[0.25,1],[0,1],[0,0.75],[0,0.5],[0,0.25]];outlineConnect=0;gradientColor=none;html=1;whiteSpace=wrap;fontSize=12;fontStyle=0;shape=mxgraph.aws4.group;grIcon=mxgraph.aws4.group_vpc;strokeColor=#8C4FFF;fillColor=none;verticalAlign=top;align=left;spacingLeft=30;fontColor=#AAB7B8;dashed=0;labelBackgroundColor=none;container=1;pointerEvents=0;collapsible=0;recursiveResize=0;" vertex="1" parent="4">
          <mxGeometry x="20" y="40" width="880" height="600" as="geometry" />
        </mxCell>
        
        <!-- Public Subnet (Web UI) -->
        <mxCell id="6" value="Public Subnet" style="points=[[0,0],[0.25,0],[0.5,0],[0.75,0],[1,0],[1,0.25],[1,0.5],[1,0.75],[1,1],[0.75,1],[0.5,1],[0.25,1],[0,1],[0,0.75],[0,0.5],[0,0.25]];outlineConnect=0;gradientColor=none;html=1;whiteSpace=wrap;fontSize=11;fontStyle=0;shape=mxgraph.aws4.group;grIcon=mxgraph.aws4.group_security_group;strokeColor=#7AA116;fillColor=#E9F3D2;verticalAlign=top;align=left;spacingLeft=30;fontColor=#248814;dashed=0;labelBackgroundColor=none;container=1;pointerEvents=0;collapsible=0;recursiveResize=0;" vertex="1" parent="5">
          <mxGeometry x="20" y="40" width="200" height="160" as="geometry" />
        </mxCell>
        
        <!-- CloudFront -->
        <mxCell id="7" value="CloudFront" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#945DF2;gradientDirection=north;fillColor=#5A30B5;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.cloudfront;" vertex="1" parent="6">
          <mxGeometry x="70" y="50" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- Private Subnet (API & Lambda) -->
        <mxCell id="8" value="Private Subnet (API)" style="points=[[0,0],[0.25,0],[0.5,0],[0.75,0],[1,0],[1,0.25],[1,0.5],[1,0.75],[1,1],[0.75,1],[0.5,1],[0.25,1],[0,1],[0,0.75],[0,0.5],[0,0.25]];outlineConnect=0;gradientColor=none;html=1;whiteSpace=wrap;fontSize=11;fontStyle=0;shape=mxgraph.aws4.group;grIcon=mxgraph.aws4.group_security_group;strokeColor=#00A4A6;fillColor=#E6F6F7;verticalAlign=top;align=left;spacingLeft=30;fontColor=#147EBA;dashed=0;labelBackgroundColor=none;container=1;pointerEvents=0;collapsible=0;recursiveResize=0;" vertex="1" parent="5">
          <mxGeometry x="20" y="230" width="400" height="160" as="geometry" />
        </mxCell>
        
        <!-- API Gateway -->
        <mxCell id="9" value="API Gateway" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#945DF2;gradientDirection=north;fillColor=#5A30B5;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.api_gateway;" vertex="1" parent="8">
          <mxGeometry x="40" y="50" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- Lambda Functions -->
        <mxCell id="10" value="Lambda&lt;br&gt;(API Backend)" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;fillColor=#ED7100;strokeColor=none;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.lambda_function;" vertex="1" parent="8">
          <mxGeometry x="170" y="50" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- Cognito -->
        <mxCell id="11" value="Cognito" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#F54749;gradientDirection=north;fillColor=#C7131F;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.cognito;" vertex="1" parent="8">
          <mxGeometry x="300" y="50" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- Private Subnet (Data) -->
        <mxCell id="12" value="Private Subnet (Data)" style="points=[[0,0],[0.25,0],[0.5,0],[0.75,0],[1,0],[1,0.25],[1,0.5],[1,0.75],[1,1],[0.75,1],[0.5,1],[0.25,1],[0,1],[0,0.75],[0,0.5],[0,0.25]];outlineConnect=0;gradientColor=none;html=1;whiteSpace=wrap;fontSize=11;fontStyle=0;shape=mxgraph.aws4.group;grIcon=mxgraph.aws4.group_security_group;strokeColor=#00A4A6;fillColor=#E6F6F7;verticalAlign=top;align=left;spacingLeft=30;fontColor=#147EBA;dashed=0;labelBackgroundColor=none;container=1;pointerEvents=0;collapsible=0;recursiveResize=0;" vertex="1" parent="5">
          <mxGeometry x="20" y="420" width="400" height="160" as="geometry" />
        </mxCell>
        
        <!-- RDS PostgreSQL -->
        <mxCell id="13" value="RDS&lt;br&gt;PostgreSQL" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#4D72F3;gradientDirection=north;fillColor=#3334B9;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.rds;" vertex="1" parent="12">
          <mxGeometry x="40" y="50" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- DynamoDB -->
        <mxCell id="14" value="DynamoDB&lt;br&gt;(Audit Logs)" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#4D72F3;gradientDirection=north;fillColor=#3334B9;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.dynamodb;" vertex="1" parent="12">
          <mxGeometry x="170" y="50" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- ElastiCache (Optional) -->
        <mxCell id="15" value="ElastiCache&lt;br&gt;Redis" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#4D72F3;gradientDirection=north;fillColor=#3334B9;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.elasticache;" vertex="1" parent="12">
          <mxGeometry x="300" y="50" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- S3 & Background Jobs (Outside VPC) -->
        <mxCell id="16" value="S3&lt;br&gt;(Images)" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#60A337;gradientDirection=north;fillColor=#277116;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.s3;" vertex="1" parent="5">
          <mxGeometry x="480" y="60" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- Lambda (Image Processing) -->
        <mxCell id="17" value="Lambda&lt;br&gt;(Image Proc)" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;fillColor=#ED7100;strokeColor=none;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.lambda_function;" vertex="1" parent="5">
          <mxGeometry x="610" y="60" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- SQS -->
        <mxCell id="18" value="SQS&lt;br&gt;(Bulk Ops)" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#F54749;gradientDirection=north;fillColor=#BC1356;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.sqs;" vertex="1" parent="5">
          <mxGeometry x="480" y="180" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- Lambda (Bulk Worker) -->
        <mxCell id="19" value="Lambda&lt;br&gt;(Bulk Worker)" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;fillColor=#ED7100;strokeColor=none;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.lambda_function;" vertex="1" parent="5">
          <mxGeometry x="610" y="180" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- EventBridge -->
        <mxCell id="20" value="EventBridge&lt;br&gt;(Scheduler)" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#F54749;gradientDirection=north;fillColor=#BC1356;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.eventbridge;" vertex="1" parent="5">
          <mxGeometry x="480" y="300" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- Lambda (Alerts) -->
        <mxCell id="21" value="Lambda&lt;br&gt;(Alerts)" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;fillColor=#ED7100;strokeColor=none;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.lambda_function;" vertex="1" parent="5">
          <mxGeometry x="610" y="300" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- CloudWatch -->
        <mxCell id="22" value="CloudWatch" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#F54749;gradientDirection=north;fillColor=#BC1356;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.cloudwatch_2;" vertex="1" parent="5">
          <mxGeometry x="480" y="420" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- X-Ray -->
        <mxCell id="23" value="X-Ray" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#F54749;gradientDirection=north;fillColor=#BC1356;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.x_ray;" vertex="1" parent="5">
          <mxGeometry x="610" y="420" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- WAF -->
        <mxCell id="24" value="WAF" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#F54749;gradientDirection=north;fillColor=#C7131F;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.waf;" vertex="1" parent="5">
          <mxGeometry x="740" y="60" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- Secrets Manager -->
        <mxCell id="25" value="Secrets&lt;br&gt;Manager" style="sketch=0;points=[[0,0,0],[0.25,0,0],[0.5,0,0],[0.75,0,0],[1,0,0],[0,1,0],[0.25,1,0],[0.5,1,0],[0.75,1,0],[1,1,0],[0,0.25,0],[0,0.5,0],[0,0.75,0],[1,0.25,0],[1,0.5,0],[1,0.75,0]];outlineConnect=0;fontColor=#232F3E;gradientColor=#F54749;gradientDirection=north;fillColor=#C7131F;strokeColor=#ffffff;dashed=0;verticalLabelPosition=bottom;verticalAlign=top;align=center;html=1;fontSize=11;fontStyle=0;aspect=fixed;shape=mxgraph.aws4.resourceIcon;resIcon=mxgraph.aws4.secrets_manager;" vertex="1" parent="5">
          <mxGeometry x="740" y="180" width="60" height="60" as="geometry" />
        </mxCell>
        
        <!-- Connections -->
        
        <!-- User to CloudFront -->
        <mxCell id="30" value="HTTPS" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="2" target="7">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- CloudFront to API Gateway -->
        <mxCell id="31" value="REST API" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="7" target="9">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- API Gateway to Lambda -->
        <mxCell id="32" value="Invoke" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="9" target="10">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- API Gateway to Cognito -->
        <mxCell id="33" value="JWT Auth" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#DD344C;strokeWidth=2;dashed=1;dashPattern=5 5;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="9" target="11">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- Lambda to RDS -->
        <mxCell id="34" value="SQL" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="10" target="13">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- Lambda to DynamoDB -->
        <mxCell id="35" value="Audit Logs" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="10" target="14">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- Lambda to ElastiCache -->
        <mxCell id="36" value="Cache" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;dashed=1;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="10" target="15">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- Lambda to S3 (presigned URL) -->
        <mxCell id="37" value="Presigned URL" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="10" target="16">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- S3 to Lambda (Image Processing) -->
        <mxCell id="38" value="S3 Event" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#E7157B;strokeWidth=2;dashed=1;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="16" target="17">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- Lambda to SQS -->
        <mxCell id="39" value="Enqueue" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="10" target="18">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- SQS to Lambda (Bulk Worker) -->
        <mxCell id="40" value="Trigger" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#E7157B;strokeWidth=2;dashed=1;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="18" target="19">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- Bulk Worker to RDS -->
        <mxCell id="41" value="Batch Update" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="19" target="13">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- EventBridge to Lambda (Alerts) -->
        <mxCell id="42" value="Schedule" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#E7157B;strokeWidth=2;dashed=1;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="20" target="21">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- Lambda (Alerts) to RDS -->
        <mxCell id="43" value="Query Stock" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="21" target="13">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- CloudFront to S3 -->
        <mxCell id="44" value="CDN Origin" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=2;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="7" target="16">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- WAF to API Gateway -->
        <mxCell id="45" value="Protect" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#DD344C;strokeWidth=2;dashed=1;dashPattern=5 5;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="24" target="9">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- Lambda to Secrets Manager -->
        <mxCell id="46" value="Fetch Secrets" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#DD344C;strokeWidth=2;dashed=1;dashPattern=5 5;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="10" target="25">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- All Lambda to CloudWatch -->
        <mxCell id="47" value="Logs" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=1;dashed=1;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="10" target="22">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <mxCell id="48" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=1;dashed=1;" edge="1" parent="1" source="17" target="22">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <mxCell id="49" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=1;dashed=1;" edge="1" parent="1" source="19" target="22">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <mxCell id="50" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=1;dashed=1;" edge="1" parent="1" source="21" target="22">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <!-- All Lambda to X-Ray -->
        <mxCell id="51" value="Traces" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=1;dashed=1;fontSize=11;labelBackgroundColor=#FFFFFF;" edge="1" parent="1" source="10" target="23">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <mxCell id="52" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=1;dashed=1;" edge="1" parent="1" source="17" target="23">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <mxCell id="53" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=1;dashed=1;" edge="1" parent="1" source="19" target="23">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
        <mxCell id="54" style="edgeStyle=orthogonalEdgeStyle;rounded=0;orthogonalLoop=1;jettySize=auto;html=1;endArrow=open;endFill=0;strokeColor=#545B64;strokeWidth=1;dashed=1;" edge="1" parent="1" source="21" target="23">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        
      </root>
    </mxGraphModel>
  </diagram>
</mxfile>
```

**Open in draw.io with AWS libraries enabled:**
https://app.diagrams.net/?libs=aws4

## Cost

### Assumptions
| Assumption | Value |
|---|---|
| Region | us-east-1 |
| Traffic | 50 concurrent admin users, 100k products, 10k API requests/day, 1k image uploads/day |
| Environments | dev, staging, prod (prod costs shown; dev/staging ~30% of prod) |
| Labor Rate | $125/hour |

### Cloud Run Cost Estimate
| Provider | Monthly Low | Monthly Expected | Monthly High | Main Drivers |
|---:|---:|---:|---:|---|
| AWS | $180 | $320 | $550 | **Compute**: Lambda (API + background jobs) ~$25; **Database**: RDS PostgreSQL db.t4g.medium Multi-AZ ~$80; **Storage**: S3 (100 GB images) + CloudFront (1 TB transfer) ~$20; **NoSQL**: DynamoDB on-demand (audit logs, bulk jobs) ~$15; **API Gateway**: 300k requests/month ~$1; **Cognito**: Free tier (50 users); **Monitoring**: CloudWatch logs/metrics ~$10; **Security**: WAF, Secrets Manager, KMS ~$15; **Cache**: ElastiCache Redis (optional) ~$15; **Data Transfer**: NAT Gateway, inter-AZ ~$30; **Backup**: RDS snapshots, S3 versioning ~$10. Low assumes no ElastiCache, minimal data transfer; High assumes ElastiCache, read replicas, higher traffic. |
| Azure | $200 | $350 | $600 | **Compute**: Azure Functions (Consumption Plan) ~$30; **Database**: Azure Database for PostgreSQL (General Purpose, 2 vCPU) ~$90; **Storage**: Blob Storage (100 GB) + Azure CDN ~$25; **NoSQL**: Cosmos DB (serverless, audit logs) ~$20; **API Management**: Basic tier ~$50; **Auth**: Azure AD B2C (free tier); **Monitoring**: Application Insights ~$15; **Security**: Azure Firewall, Key Vault ~$20; **Cache**: Azure Cache for Redis (optional) ~$20; **Data Transfer**: VNet, inter-region ~$35; **Backup**: Automated backups ~$10. Low assumes no cache, minimal transfer; High assumes cache, geo-replication. |
| GCP | $190 | $330 | $580 | **Compute**: Cloud Functions (2nd gen) ~$28; **Database**: Cloud SQL for PostgreSQL (db-custom-2-7680, HA) ~$85; **Storage**: Cloud Storage (100 GB) + Cloud CDN ~$22; **NoSQL**: Firestore (serverless, audit logs) ~$18; **API Gateway**: API Gateway (300k requests) ~$3; **Auth**: Firebase Auth (free tier); **Monitoring**: Cloud Logging + Cloud Trace ~$12; **Security**: Cloud Armor, Secret Manager ~$18; **Cache**: Memorystore for Redis (optional) ~$18; **Data Transfer**: VPC, egress ~$32; **Backup**: Automated backups ~$12. Low assumes no cache, minimal egress; High assumes cache, multi-region. |

### Labor Cost Estimate
| Workstream | One-Time Hours | One-Time Cost | Monthly Maintenance Hours | Monthly Maintenance Cost |
|---|---:|---:|---:|---:|
| Architecture and planning | 40 | $5,000 | 4 | $500 |
| Implementation | 320 | $40,000 | 0 | $0 |
| Testing and hardening | 80 | $10,000 | 8 | $1,000 |
| Operations and support | 0 | $0 | 20 | $2,500 |
| **Total** | **440** | **$55,000** | **32** | **$4,000** |

**Workstream Details**:
- **Architecture and planning**: Initial design (this document), wireframes, database schema, API contracts, infrastructure planning; ongoing refinement and tech debt review.
- **Implementation**: Frontend (React SPA, 120 hours), Backend (Express API, 100 hours), Database (Prisma schema, migrations, 40 hours), Infrastructure (CDK/Terraform, 40 hours), Background jobs (Lambda functions, 20 hours).
- **Testing and hardening**: Unit tests (40 hours), integration tests (20 hours), E2E tests (10 hours), security review (10 hours).
- **Operations and support**: Monitoring, incident response, backup/restore drills, dependency updates, minor bug fixes.

### Cost Risks
- **RDS scaling**: If product catalog grows beyond 1M products or concurrent users exceed 50, RDS instance may need to scale up (db.r6g.large ~$200/month) or add read replicas (+$80/month each).
- **Image storage growth**: If image uploads exceed 1k/day, S3 storage costs will grow linearly (~$0.023/GB/month); lifecycle policies to Glacier can reduce costs by 80% for old images.
- **DynamoDB on-demand pricing**: Audit logs and bulk operation jobs use on-demand billing; if write volume spikes (e.g., bulk operations on 100k products), costs could increase significantly. Consider provisioned capacity if usage is predictable.
- **ElastiCache optional**: If PostgreSQL full-text search meets performance requirements (< 500ms), ElastiCache can be deferred, saving ~$15-20/month.
- **Data transfer costs**: NAT Gateway and inter-AZ data transfer can be expensive if Lambda functions make frequent external API calls or large S3 uploads. Use VPC endpoints for S3/DynamoDB to reduce costs.
- **Multi-environment overhead**: Dev and staging environments add ~30% to cloud costs; consider using smaller instance types (db.t4g.micro for dev) or shared environments to reduce costs.

### Optimization Opportunities
- **Lambda provisioned concurrency**: Only enable for critical endpoints (e.g., product list API) to reduce cold start latency; costs ~$15/month per function but improves UX.
- **RDS read replicas**: Add read replicas only if read-heavy queries (product list, search) cause RDS CPU > 80%; defer until needed.
- **S3 lifecycle policies**: Transition old image versions to Glacier after 90 days (saves 80% storage cost); transition audit log archives to Glacier Deep Archive after 1 year (saves 95%).
- **DynamoDB provisioned capacity**: If audit log write volume is predictable (e.g., 1k writes/day), switch from on-demand to provisioned capacity (saves ~30%).
- **CloudFront cache optimization**: Increase cache TTL for product images (1 year) and use versioned URLs to reduce S3 requests and data transfer costs.
- **Spot instances for background jobs**: Use Lambda (serverless) instead of ECS Fargate for background jobs to avoid always-on costs.
- **Reserved instances**: If RDS usage is stable for 1+ years, purchase reserved instances (saves ~40% vs. on-demand).
- **Savings Plans**: AWS Compute Savings Plans can reduce Lambda and Fargate costs by 17-20% with 1-year commitment.

**Total Cost of Ownership (TCO) — First Year**:
- **Cloud (AWS, prod only)**: $320/month × 12 = $3,840
- **Cloud (dev + staging)**: $320 × 0.3 × 12 = $1,152
- **Labor (one-time)**: $55,000
- **Labor (monthly maintenance)**: $4,000/month × 12 = $48,000
- **Total Year 1**: $108,000 (cloud + labor)

**Ongoing Annual Cost (Year 2+)**:
- **Cloud (all environments)**: $4,992/year
- **Labor (maintenance)**: $48,000/year
- **Total Year 2+**: $53,000/year